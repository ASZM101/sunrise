// Adds nav bar to all pages with nav element
if (document.querySelector('nav')) {
    document.querySelector('nav').innerHTML = `<a href="index.html">Home</a><a href="projects.html">Projects</a><a href="photos.html">Photos</a>`
}